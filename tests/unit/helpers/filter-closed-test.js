import { filterClosed } from 'help-queue/helpers/filter-closed';
import { module, test } from 'qunit';

module('Unit | Helper | filter closed');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterClosed([42]);
  assert.ok(result);
});
